version: 2
jobs:
  build:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run: docker build -t 4p00rv/py_server .

workflow:
  version: 2
  build_and_deploy:
    jobs:
      - build:
          filters:
            branches:
              only: master
